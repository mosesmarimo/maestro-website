# PostgreSQL connection string (Neon, Supabase, RDS, or local docker-compose).
# Vercel Marketplace databases inject POSTGRES_URL automatically.
POSTGRES_URL=postgres://maestro:maestro@localhost:5432/maestro_website
